# API Configuration
# Leave empty to auto-detect based on current hostname
# REACT_APP_API_URL=http://localhost:8085

# For Windows users, if localhost doesn't work, try:
# REACT_APP_API_URL=http://127.0.0.1:8085
